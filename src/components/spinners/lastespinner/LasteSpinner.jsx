import React from 'react';
import './lastespinner.scss';

export const LasteSpinner = () => {
    return <span aria-label='venter på svar' className='lastespinner fa fa-spinner fa-spin'></span>;
};

export default LasteSpinner;